{
  "name": "@boid-wasm-sim/wasm",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "asc src/core/index.ts --target release --outFile build/boid.wasm --exportRuntime",
    "build:debug": "asc src/core/index.ts --target debug --outFile build/boid.wasm --exportRuntime --sourceMap",
    "dev": "npm run build:debug -- --watch"
  },
  "devDependencies": {
    "assemblyscript": "^0.28.3"
  }
}